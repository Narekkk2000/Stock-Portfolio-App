import AddStock from "./addStock/AddStock";
import styles from "./header.module.css";

const Header = () => {
  return (
    <header>
        <AddStock />
    </header>
  );
};

export default Header;
